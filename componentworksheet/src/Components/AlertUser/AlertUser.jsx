import React from 'react';

const AlertUser = props => {
  return <button onClick={props.Display}>Click Me</button>;
};

export default AlertUser;

// function DisplayAlert(props) {
//   alert('devCodeCamp');
// }
